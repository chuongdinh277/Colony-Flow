using UnityEngine;

namespace ColonyFlow
{
    public sealed class MainMenuUI : MonoBehaviour
    {
        private void Start()
        {
            // Flow is now entirely handled by GameManager.Start()
        }
    }
}
